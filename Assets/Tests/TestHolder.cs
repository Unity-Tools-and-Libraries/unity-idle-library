using System.Collections;
using System.Collections.Generic;
using io.github.thisisnozaku.idle.framework.ui;
using UnityEngine;

public class TestHolder : EngineHolder
{
    // Start is called before the first frame update
    void Start()
    {
        engine = new io.github.thisisnozaku.idle.framework.Engine.IdleEngine();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
